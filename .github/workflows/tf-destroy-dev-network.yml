name: tf-destroy-dev-network

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type EXACTLY: destroy dev network"
        required: true
        default: ""
permissions:
  id-token: write
  contents: read

jobs:
  destroy:
    if: ${{ github.event.inputs.confirm == 'destroy dev network' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: envs/dev/network
    steps:
      - uses: actions/checkout@v4

      - name: Debug refs
        run: |
          echo "GITHUB_REPOSITORY=$GITHUB_REPOSITORY"
          echo "GITHUB_REF=$GITHUB_REF"

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::817041139478:role/GitAdmin
          aws-region: ap-southeast-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init (same backend)
        run: terraform init -upgrade

      - name: Preview Destroy (optional but recommended)
        run: terraform plan -destroy

      - name: Terraform Destroy (no prompt)
        run: terraform destroy -auto-approve
